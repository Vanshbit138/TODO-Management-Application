# Step 2 — AI/QA Workflows

This document outlines basic developer workflows for code quality in this repository.

- Pre-commit hooks: run linters/formatters before committing
- Unit tests: pytest structure ready under `tests/`
- Type checking: mypy (to be configured in later steps)
- Style: black + isort + flake8

