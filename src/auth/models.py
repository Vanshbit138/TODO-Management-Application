"""
Authentication models module.

This module defines SQLAlchemy models for user authentication and authorization.
It includes User model with authentication-related fields, roles, permissions,
and any other database entities related to user management and access control.
"""
